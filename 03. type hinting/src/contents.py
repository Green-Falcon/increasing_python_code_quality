class Player:
    def __init__(self, age):
        self.age = age


def mean(a: int, b: int, c: int) -> float:
    return (a + b + c) / 3


print(mean(1, 2, 3))

a: int = 100
b: str = "hello"
